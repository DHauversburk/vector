# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files (NEVER commit secrets)
.env
.env.local
.env.production
.env.staging

# Vercel
.vercel

# Claude Code / local agent tool state (per-user, not shared)
.claude/

# Test coverage output (generated)
coverage/

# GitHub CLI scratch output
check-runs.json

# Supabase CLI local cache (per-machine, not shared)
supabase/.temp/
